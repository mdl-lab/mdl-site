server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # サイト全体に Basic 認証(kpro と同方式)
    auth_basic "MDL staging";
    auth_basic_user_file /etc/nginx/htpasswd;

    location /healthz {
        auth_basic off;
        return 200 "ok\n";
    }
}
